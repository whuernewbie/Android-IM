
git config --global <key> <value> // 设置信息

git config --global -l // 显示配置信息

git help -a // 查询帮助

git init // 初始化仓库

git add

git commit -m <提交信息>

git status // 查看状态

git diff // 查看不同，与上次提交后发生的变动

git log // 提交日志 // work tree

git reset --hard HEAD^ // 回退至上一版本 // HEAD~n 回退至前 n 个版本

git reflog // 查看版本变动记录

// 回退版本也可以去 .git/logs/HEAD/refs/heads 中 查看 master 文件
